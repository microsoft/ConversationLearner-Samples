export declare class Server {
    private static server;
    private static InitClient();
    private static HTML2Error(htmlText);
    private static HandleError(response, err);
    static Init(): void;
}
