import { BlisMemory } from '../BlisMemory';
import { EntityBase, MemoryValue } from 'blis-models';
export declare class ClientMemoryManager {
    blisMemory: BlisMemory;
    private entities;
    constructor(memory: BlisMemory, entities: EntityBase[]);
    FindEntity(entityName: string): EntityBase;
    RememberEntityAsync(entityName: string, value: string): Promise<void>;
    ForgetEntityAsync(entityName: string, value?: string): Promise<void>;
    CopyEntityAsync(entityNameFrom: string, entityNameTo: string): Promise<void>;
    EntityValueAsync(entityName: string): Promise<string>;
    EntityValueAsPrebuiltAsync(entityName: string): Promise<MemoryValue[]>;
    EntityValueAsListAsync(entityName: string): Promise<string[]>;
    GetFilledEntitiesAsync(): Promise<string[]>;
    AppNameAsync(): Promise<string>;
}
